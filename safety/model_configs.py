# The official model identifier for each model in the safety subset.
#
# A shadow endpoint serves the same claimed model under its own name, so that id
# is supplied per run with --model-id rather than tabulated here.
OFFICIAL_MODEL_IDS = {
    "GPT-5-Mini": "gpt-5-mini-2025-08-07",
    "Gemini-2.5-Flash": "gemini-2.5-flash",
    "DeepSeek-V3.2-Chat": "deepseek-chat",
}
